disabled_rules:
    - line_length
    - identifier_name
    - type_name
    - function_body_length

trailing_whitespace:
    severity: error

trailing_comma:
    severity: error
    mandatory_comma: true

included:
    - Sources/

excluded:
    - Tests/
    - Tuist/
